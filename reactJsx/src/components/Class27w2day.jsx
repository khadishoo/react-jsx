import React, { useState } from 'react'

export default function Class27w2day() {
  // let loggedin = true


  // const [darkMode, setDarkMode] = useState(false)
  // const [showInfo, setShowInfo] = useState(false)
  return (
    <div
      // style={{
      //   backgroundColor: darkMode ? "black" : "white",
      //   color: darkMode ? "white" : "black",
      //   height: "100vh",
      //   display: "flex",
      //   flexDirection: "column",
      //   alignItems: 'center',
      //   justifyContent: "center"
      // }}
    >



      {/* <div>
       <h2>{darkMode ? "qara rejim kosildy!" : "jarik rejim kosildy!"}</h2>
      </div>

      <button onClick={()=> setDarkMode(!darkMode)}>{darkMode ? "jarik rejimge auistiru" : "qara rejimga auistiru"}</button>

      <button onClick={() => setShowInfo(!showInfo)}>
        {showInfo ? "jinau" : "Qosymsha akparat"}
      </button>

      {showInfo && <p>qosimsha aqparat</p>} */}



      {/* {loggedin ? "siz juege kirdiniz" : "siz juege kire almadiniz"}
      {loggedin && "siz juege kirdiniz"} */}
    </div>
  )
}
